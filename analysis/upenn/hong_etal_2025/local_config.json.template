{
    "_comment": "Copy to local_config.json and set paths for this machine. Relative paths are resolved from the repo root. The defaults store data within the repo (git-ignored). data_dir and analysis_dir must be non-empty and different. data_repo is only read by download_data.py.",
    "data_dir": "local/data/upenn/hong_etal_2025",
    "analysis_dir": "local/analysis/upenn/hong_etal_2025",
    "data_repo": "https://osf.io/k27js"
}
